% layout 'default';
<h1 class="page-title">Pagine</h1>
<section class="card">
  <ul>
    % for my $p (@$pages) {
      <li>
        <strong><%= $p->{title} // '(senza titolo)' %></strong>
        — <a href="<%= $p->{url} %>" target="_blank">link</a>
        <form method="POST" action="/pages/<%= $p->{id} %>/delete" style="display:inline-block; margin-left:10px;">
          <button type="submit" class="btn">Elimina</button>
        </form>
      </li>
    % }
  </ul>
</section>
